# Reading the documents. erp.py itself stays standard-library only.
pdfplumber>=0.11,<0.12
pdf2image>=1.17,<2
pytesseract>=0.3.13,<0.4
rapidfuzz>=3.10,<4

# Building the synthetic corpus the tests run against.
reportlab>=4.2
pytest>=8.3
